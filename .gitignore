# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build artifacts
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Tauri runtime caches
src-tauri/.cache

# Chronicle transcripts safeguard — should never land in this repo, but if
# someone runs the gadget against a clone with PWD inside the submodule, we
# refuse to track them.
.zmuuzn-cockpit-transcripts/
